---
# This playbook uses the win_ping module to test connectivity to Windows hosts
- name: Ping 
  hosts: windows 
  become: yes

  tasks:
  - name: notepad 
    win_chocolatey:
      name: 
      - notepadplusplus
      - notepadplusplus.install
      state: present
